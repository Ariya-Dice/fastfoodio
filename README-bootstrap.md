# نسخه Bootstrap برنامه فست‌فودیو

این نسخه از برنامه فست‌فودیو با استفاده از **Bootstrap 5** طراحی و پیاده‌سازی شده است.

## تفاوت‌ها با نسخه اصلی

### 🎨 فریمورک CSS
- **نسخه اصلی**: Tailwind CSS
- **نسخه Bootstrap**: Bootstrap 5 + Bootstrap Icons

### 📱 طراحی ریسپانسیو
- استفاده از سیستم Grid Bootstrap
- کلاس‌های responsive مانند `col-12 col-md-6 col-lg-4`
- کامپوننت‌های آماده Bootstrap مانند Navbar, Card, Button

### 🎯 ویژگی‌ها
- تمام ویژگی‌های نسخه اصلی حفظ شده
- طراحی ساده‌تر و تمیزتر
- استفاده از آیکون‌های Bootstrap Icons
- RTL (راست‌به‌چپ) کاملاً پشتیبانی می‌شود

## ساختار فایل‌ها

```
fastfoodio-bootstrap/
├── App-simple.tsx          # کامپوننت اصلی ساده‌سازی شده
├── data/mockData.ts        # داده‌های نمونه
├── public/images/          # تصاویر محلی
├── index.html             # فایل HTML با Bootstrap
├── index.tsx              # نقطه ورود React
├── package.json           # تنظیمات npm
├── tsconfig.json          # تنظیمات TypeScript
└── vite.config.ts         # تنظیمات Vite
```

## اجرای برنامه

```bash
cd fastfoodio-bootstrap
npm install
npm run dev
```

## ویژگی‌های کلیدی

### 🏠 صفحه اصلی (Home)
- هدر با ناوبری Bootstrap
- بخش Hero با تصویر پس‌زمینه
- کارت‌های ویژگی با آیکون‌ها
- فوتر ساده

### 🏪 انتخاب شعبه (Branches)
- لیست شعبه‌ها در کارت‌های Bootstrap
- نمایش وضعیت باز/بسته
- ریسپانسیو کامل

### 🍽️ منو و سبد خرید (Menu/Cart)
- دسته‌بندی محصولات به صورت تب‌های افقی
- کارت‌های محصول با تصویر و جزئیات
- سبد خرید کشویی (Drawer)
- دکمه شناور برای موبایل

## مزایای استفاده از Bootstrap

1. **سرعت توسعه**: کامپوننت‌های آماده و تست شده
2. **سازگاری مرورگر**: پشتیبانی گسترده از مرورگرها
3. **قابل تنظیم**: امکان سفارشی‌سازی با CSS Variables
4. **دسترسی‌پذیری**: رعایت استانداردهای WCAG
5. **جامعه بزرگ**: پشتیبانی و منابع فراوان

## تکنولوژی‌های استفاده شده

- **React 19**: کتابخانه رابط کاربری
- **Bootstrap 5.3.0**: فریمورک CSS
- **Bootstrap Icons**: مجموعه آیکون‌ها
- **TypeScript**: تایپ اسکریپت برای ایمنی کد
- **Vite**: ابزار ساخت سریع
- **VazirMatn**: فونت فارسی

## نکات پیاده‌سازی

- تمام تصاویر به صورت محلی ذخیره شده‌اند
- از CDN Bootstrap برای پایداری بیشتر استفاده شده
- طراحی کاملاً ریسپانسیو (موبایل، تبلت، دسکتاپ)
- پشتیبانی کامل از زبان فارسی و RTL
